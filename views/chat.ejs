<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  <link rel="stylesheet" href="/main.css">
  
</head>
<body style="background-color: gainsboro;">
  <%- include('nav.ejs') %> 
  <h3><%= user.username%>님의 체팅방</h3>
  <p class="lgout">로그아웃</p>
  <div class="white-box">
    <% for (var i = 0; i < post.length; i++){ %>
      <div class="list-box">
        <a href="/detail/<%= post[i]._id %>"><%= post[i].parentname %></a>
        
        <div class="edit-delete-container">
          <span class="delete" data-id="<%= post[i]._id %>">🗑️</span>
          
          
        </div>
      </div>
    <% } %> 
  </div>

  <script>
    document.querySelector('.search-send').addEventListener('click', function(){
      let ind = document.querySelector('.search').value
      location.href = '/search?val=' + ind
    })
  </script>
  <script>
    
    for (let i = 0; i < '<%=post.length %>'; i++) {
      document.querySelectorAll('.delete')[i].addEventListener('click', function (e) {
        fetch('/delete?docid=' + e.target.dataset.id, {
          method: 'DELETE'
        })
          .then((r) => {
            if (r.ok) {
              // 응답이 성공적인 경우
              return r.text();
            } else {
              // 응답이 실패한 경우
              throw new Error('삭제 실패: ' + r.statusText);
            }
          })
          .then((r) => {
            // 삭제가 성공적으로 이루어진 경우
            e.target.parentElement.parentElement.style.display = 'none';
          })
          .catch((error) => {
            // 삭제에 실패하거나 서버 에러가 발생한 경우
            console.error(error);
            alert("작성자만 삭제 가능하니 건들지마")
            // 여기서 에러를 처리하거나 특별한 동작을 수행할 수 있습니다.
          });
      });
    }
  </script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>